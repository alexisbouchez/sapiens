{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "api": "cd api && yarn start:dev",
    "web": "cd web && yarn dev",
    "dev": "concurrently \"yarn api\" \"yarn web\""
  },
  "dependencies": {
    "concurrently": "^7.2.2"
  }
}
